# Avalon Night

Mobile-first Avalon lobby + role reveal.

## Run locally

```bash
npm install
cd worker && npm install
cd worker && npm run dev
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8787 npm run dev
```

## Tests

```bash
cd worker && npm test
```
